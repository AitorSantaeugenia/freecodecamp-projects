function palindrome(str) {
	//regExpr to remove characters we don't want
	var regEx = /[^A-Za-z0-9]/g;

	//starting to loop
	for (let i = 0; i < str.length; i++) {}

	return true;
}

console.log(palindrome('eye'));
